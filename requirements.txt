# India Commerce SignalForge — capstone_final dependencies.
# Runtime LLM inference = Groq only. Open models/datasets/Hub = Hugging Face only.
# Web evidence = Tavily. Experiment tracking = Weights & Biases (optional, offline fallback).
groq>=0.11
anthropic>=0.39
requests>=2.31
beautifulsoup4>=4.12
tavily-python>=0.5
chromadb>=0.5
sentence-transformers>=2.7
transformers>=4.40
torch>=2.2
sentencepiece>=0.2
tiktoken>=0.7
peft>=0.19
accelerate>=1.0
datasets>=2.19
scikit-learn>=1.4
xgboost>=2.0
numpy>=1.26
matplotlib>=3.8
langchain-core>=0.2
gradio>=4.0
pydantic>=2.6
huggingface-hub>=0.23
# Optional — experiment tracking. The code falls back to offline JSONL when absent.
wandb>=0.17
